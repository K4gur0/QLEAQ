{% extends 'home/index.html.twig' %}

{% block title %}{% include 'includes/qleaq_title.html.twig' %}Accueil Propiétaire{% endblock %}

{% block content %}
    <section>

        {% if is_granted('ROLE_PROPRIO') %}



            <section class="hero is-medium is-info is-bold">
                <div class="hero-body">
                    <div class="container">
                        <h1 class="title">
                            Bonjour {{ app.user.raisonsocial }} !
                        </h1>
                        <h2 class="subtitle">
                            Bienvenue sur votre espace Propriétaire
                        </h2>
                    </div>
                </div>
            </section>

            <section class="section">

                {% include 'includes/flash_message.html.twig' %}

                <a class="button is-success is-outlined" href="{{ path('proprio_presentation') }}"> <strong>Accueil Propriétaire</strong></a>
                <a class="button is-info is-outlined" href="{{ path('proprio_add_annonce') }}"><strong>Ajouter une annonce</strong></a>

                <h2 class="subtitle is-3">Mes annonces :</h2>
                <hr style="border:black 1px solid">


{#                #}{#                        {{ form_row(nomadeForm.nom, {'attr' : {'placeholder' : 'Entrez votre Nom'}#}
{#                #}{#                        }) }}#}
{#                {% if form_errors(annonceForm) != null %}#}
{#                    <div class="notification is-danger">#}
{#                        <button class="delete" onclick="this.parentElement.parentElement.removeChild(this.parentElement);"></button>#}
{#                        {{ form_errors(annonceForm) }}#}
{#                    </div>#}
{#                {% endif %}#}


                    {% include('includes/InfoAnnonce.html.twig') %}



            </section>


        {% else %}

            <section class="hero is-dark is-bold is-large">
                <div class="hero-body">
                    <div class="container">
                        <h1 class="title">
                            Vous n'êtes pas éligible à cette page
                        </h1>
                        <a class="button is-large" style="margin: auto" href="{{ path('home') }}">Revenir à l'Accueil</a>
                    </div>
                </div>
            </section>

        {% endif %}

    </section>





{% endblock %}